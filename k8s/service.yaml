apiVersion: v1
kind: Service
metadata:
  name: finance-app-svc
  namespace: default
spec:
  type: NodePort
  selector:
    app: finance-app
  ports:
  - name: http
    port: 80
    targetPort: 80
    nodePort: 30080
