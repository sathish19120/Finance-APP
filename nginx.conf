server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html finance.html;
    location / {
        try_files $uri $uri/ /index.html;
    }
    gzip on;
    gzip_types text/html text/css application/javascript;
}
